import React from 'react';

function Jobs(props) {
    return (
        <div className="job-container">
            {CharacterData.map({charData => (
                
            )})}

        </div>
    );
}

export default Jobs;